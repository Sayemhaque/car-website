import Header from "../../components/Header/Header";

const Home = () => {
    return (
        <section className="bg-indigo-100 h-screen">
            <Header/>
        </section>
    );
};

export default Home;